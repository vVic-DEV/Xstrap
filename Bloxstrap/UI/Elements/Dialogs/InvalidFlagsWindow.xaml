<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.InvalidFlagsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
    xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Dialogs"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    mc:Ignorable="d"
    Title="Invalid FastFlags"
    Width="600"
    MinHeight="400"
    SizeToContent="Height"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.Background>
            <DynamicResource ResourceKey="WindowBackgroundGradient" />
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Padding="8"
                     ShowMinimize="False" ShowMaximize="False" CanMaximize="False"
                     Title="Invalid FastFlags" />

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- New Descriptive Header -->
            <TextBlock Grid.Row="0"
                       Text="Fake / Removed FastFlags"
                       FontWeight="Bold"
                       FontSize="16"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       Margin="0 0 0 12" />

            <!-- Scrollable TextBox -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          MinHeight="300">
                <TextBox x:Name="InvalidFlagsTextBox"
                         FontFamily="Consolas"
                         FontSize="14"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         BorderThickness="1"
                         Padding="8"
                         Background="{DynamicResource PrimaryBackgroundColor}"
                         Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </ScrollViewer>
        </Grid>

        <!-- Footer with Close button -->
        <Border Grid.Row="2" Padding="15" Background="{DynamicResource PrimaryBackgroundColor}">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CloseButton" MinWidth="100" Content="Close" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</base:WpfUiWindow>